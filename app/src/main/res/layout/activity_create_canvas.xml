<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="bws.customerrelation.GUI.CreateCanvasActivity">

    <LinearLayout
        android:id="@+id/datalinearlayout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:id="@+id/spinnersLayout"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Spinner
                android:id="@+id/countrySpinner"
                android:layout_width="180dp"
                android:layout_height="35dp"
                android:background="@android:drawable/btn_dropdown"
                android:dropDownWidth="180dp"
                android:spinnerMode="dropdown"></Spinner>

            <Spinner
                android:id="@+id/TypeOfVisitSpinner"
                android:layout_width="fill_parent"
                android:layout_height="35dp"
                android:background="@android:drawable/btn_dropdown"
                android:dropDownWidth="180dp"
                android:spinnerMode="dropdown"></Spinner>

        </LinearLayout>

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:orientation="horizontal">


            <Spinner
                android:id="@+id/activitySpinner"
                android:layout_width="180dp"
                android:layout_height="35dp"
                android:background="@android:drawable/btn_dropdown"
                android:dropDownWidth="180dp"
                android:spinnerMode="dropdown"></Spinner>

            <Spinner
                android:id="@+id/businessAreaSpinner"
                android:layout_width="fill_parent"
                android:layout_height="35dp"
                android:background="@android:drawable/btn_dropdown"
                android:dropDownWidth="180dp"
                android:spinnerMode="dropdown"></Spinner>

        </LinearLayout>

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:orientation="horizontal">


            <Spinner
                android:id="@+id/followUpSpinner"
                android:layout_width="180dp"
                android:layout_height="35dp"
                android:background="@android:drawable/btn_dropdown"
                android:dropDownWidth="180dp"
                android:spinnerMode="dropdown"></Spinner>

            <Spinner
                android:id="@+id/typeOfTransportSpinner"
                android:layout_width="fill_parent"
                android:layout_height="35dp"
                android:background="@android:drawable/btn_dropdown"
                android:dropDownWidth="180dp"
                android:spinnerMode="dropdown"></Spinner>

        </LinearLayout>

        <EditText
            android:id="@+id/txtSubject"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:hint="@string/lblSubject"
            android:maxLength="30" />
    </LinearLayout>



    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@+id/btnSave"
        android:layout_below="@+id/datalinearlayout">

        <EditText
            android:id="@+id/newCanvas"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:ems="10"
            android:hint="@string/enterText" />
    </ScrollView>

    <Button
        android:id="@+id/btnSave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:text="@string/btnSave" />

</RelativeLayout>
