<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bws.customerrelation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".GUI.LoginActivity"
            android:label="@string/app_name">

            <!-- android:theme="@style/MyActionBar" > -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GUI.MainActivity"
            android:label="@string/title_activity_main"></activity>
        <activity
            android:name=".GUI.ClientActivity"
            android:label="@string/title_activity_client"></activity>
        <activity
            android:name=".GUI.ClientDataActivity"
            android:label="@string/title_activity_client_data"></activity>
        <activity
            android:name=".GUI.CanvasActivity"
            android:label="@string/title_activity_canvas"></activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
